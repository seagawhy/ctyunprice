# 天翼云报价工具 - 代码更新与部署指南

> 最后更新：2026-05-13

---

## 一、本地 Git 提交

### 1.1 查看当前状态

```bash
git status
```

查看有哪些文件被修改或新增。

### 1.2 添加文件到暂存区

**方式A：添加所有修改的文件**
```bash
git add .
```

**方式B：只添加指定文件（推荐，更安全）**
```bash
git add index.html js/app.js server.js
```

### 1.3 提交到本地仓库

```bash
git commit -m "描述本次修改的内容"
```

提交信息示例：
- `"修复了首次下载空文件的Bug"`
- `"新增了状态持久化功能"`
- `"优化UI样式"`

### 1.4 常用 Git 命令速查

| 命令 | 说明 |
|------|------|
| `git status` | 查看工作区状态 |
| `git diff` | 查看具体改了什么 |
| `git log --oneline -5` | 查看5条最近提交 |
| `git checkout -- <文件>` | **撤销某个文件的修改**（慎用） |

---

## 二、同步到 GitHub

### 2.1 推送到远程

```bash
git push origin main
```

如果推送时提示用户名/密码错误，可能需要配置 GitHub 认证（SSH Key 或 Personal Access Token）。

### 2.2 首次推送（如果还没有关联远程仓库）

```bash
# 先在 GitHub 上创建一个新仓库，然后执行：
git remote add origin https://github.com/<你的用户名>/<仓库名>.git
git push -u origin main
```

---

## 三、更新线上服务器

### 3.1 线上服务器信息

| 项目 | 值 |
|------|-----|
| 地址 | http://159.75.243.119:3000 |
| 用户名 | `ubuntu`（小写！） |
| 密码 | `*why2026#` |
| 部署路径 | `/opt/ctyun-quote/` |
| 部署方式 | Docker Compose |

### 3.2 更新步骤

#### 方法一：使用 Node.js 部署脚本（推荐）

项目已内置部署依赖，只需运行：

```powershell
node _deploy.js
```

该脚本会自动完成：上传文件 → 远程重建 Docker 容器 → 等待启动 → 健康检查。

> **注意**：首次使用前需确认已安装 npm 依赖（`npm install ssh2`）。

#### 方法二：手动操作（了解原理）

**Step 1 - 上传文件到服务器**
```powershell
scp -r . ubuntu@159.75.243.119:/opt/ctyun-quote/
```
（需要手动输入密码）

**Step 2 - SSH 登录并重建容器**
```bash
ssh ubuntu@159.75.243.119
cd /opt/ctyun-quote
sudo docker compose up -d --build
```

**Step 3 - 验证**
```bash
curl http://localhost:3000/api/health
# 返回 {"status":"ok"} 表示成功
```

### 3.3 完整操作流程（从零到上线）

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  1. 本地修改   │ --> │  2. Git提交   │ --> │  3. 推送GitHub │ --> │  4. 部署线上  │
│  修改代码文件   │     │ git commit   │     │ git push     │     │ node _deploy │
│              │     │              │     │              │     │              │
└──────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
       ▲                                                               │
       │                    浏览器打开                                  │
       └───────────────── http://159.75.243.119:3000 ◄────────────────┘
                          验证功能正常
```

---

## 四、注意事项

### ⚠️ 必须遵守的规则

1. **先本地测试，再上线** — 修改代码后先在本地 `node server.js` 启动预览，确认功能正常后再部署
2. **JS 版本号更新** — 如果修改了 `js/app.js` 或其他 JS 文件，记得同步更新 `index.html` 中对应的版本号参数（如 `?v=20260513v17`），否则浏览器可能加载缓存的旧文件
3. **不要直接在线上修改代码** — 始终在本地修改 → Git 提交 → 部署上线
4. **Git 提交信息要清晰** — 方便后续追溯问题

### 📋 JS 文件版本号清单（index.html 中）

修改以下任一文件时，需递增对应版本号：

| 文件 | 版本号参数位置 | 当前版本 |
|------|---------------|---------|
| `js/app.js` | `<script src="js/app.js?v=...">` | v20260513v16 |

---

## 五、常见问题

**Q: 推送 GitHub 时提示认证失败？**
A: 需要配置 Personal Access Token。在 GitHub → Settings → Developer settings → Personal access tokens 创建一个，之后用 token 作为密码。

**Q: 部署后页面还是旧内容？**
A: 浏览器缓存问题，按 `Ctrl+Shift+R` 强制刷新。

**Q: `_deploy.js` 找不到或报错？**
A: 确认 `node_modules/ssh2` 存在，若不存在则执行 `npm install ssh2`。

**Q: Docker 构建失败？**
A: SSH 登录服务器执行 `sudo docker compose logs` 查看错误日志。
